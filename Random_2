from numpy import*
import numpy as np
from random import *
import itertools


eingabe = input()

for x in range (1,int(eingabe)):
	y=randint(1,20)
	z = sorted(np.random.choice(20,y))
	print(np.asarray(z))
	




